extends layout.pug

block content
  if repositories
    each repo in repositories
      div.card
        div.card-content

          div.content
            h2
              a(href=repo.url)= repo.name
            p= repo.description
            span= repo.count + ' stars'